USE registry;

CREATE TABLE users (
    id int NOT NULL AUTO_INCREMENT,
    name varchar(255),
    email varchar(255),
    PRIMARY KEY (id)
);

UPDATE mysql.user SET host = '%' WHERE user = 'demo';
UPDATE mysql.db SET host = '%' WHERE user = 'demo';
FLUSH PRIVILEGES;
